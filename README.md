# kennti-content-calendar

**Public** static Vercel gallery — preview/render outputs of the `kennti-social-publishing` pipeline
(bilingual AR/DE medical social content: rendered cards, `calendar.json`, media assets). No application
logic, no secrets. Public visibility appears **intentional** (a shareable gallery), but medical content
labels are world-readable.

See the open issue to confirm public visibility + long-term fate. Archival/visibility unchanged by this PR.
